import * as React from 'react'
import Typography from 'material-ui/Typography'

export default class extends React.PureComponent {
  render() {
    return <Typography variant="title">About page</Typography>
  }
}
